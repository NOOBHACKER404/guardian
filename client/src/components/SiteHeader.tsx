import {Menu, Search, ShieldCheck, X, ArrowRight} from 'lucide-react';
import {Link} from '@/components/HashLink'; import {useLocation} from 'wouter';
import {useState} from 'react';
export default function SiteHeader({onOpenAccount}:{onOpenAccount:()=>void}){const [open,setOpen]=useState(false);const [,go]=useLocation();const links=[['About','/about'],['Risk database','/search'],['OrgGuard','/orgguard'],['Pricing','/pricing'],['Contact','/contact']];return <><header className="site-header"><Link className="brand" href="#/"><span className="brand-mark"><ShieldCheck size={19}/></span><span>GUARDIAN<small>PBVS / ORGGUARD</small></span></Link><nav className="desktop-nav">{links.map(([t,h])=><Link key={h} href={h}>{t}</Link>)}</nav><div className="header-actions"><button className="icon-button" onClick={()=>go('/search')} aria-label="Search"><Search size={18}/></button><button className="outline-button desktop-only" onClick={onOpenAccount}>Create account <ArrowRight size={15}/></button><button className="menu-button" onClick={()=>setOpen(!open)}>{open?<X/>:<Menu/>}</button></div></header>{open&&<div className="mobile-menu"><div className="mobile-menu-inner">{links.map(([t,h])=><Link key={h} href={h} onClick={()=>setOpen(false)}>{t}<span>›</span></Link>)}<button className="mobile-verify" onClick={()=>{setOpen(false);onOpenAccount();}}>Create account <ArrowRight size={16}/></button></div></div>}</>}
